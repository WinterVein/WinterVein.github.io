{
  "name": "photo-gallery",
  "version": "2.1.0",
  "description": "Recursive photo gallery. Pure Node.js — no Ruby, no Jekyll.",
  "type": "module",
  "private": true,
  "scripts": {
    "compress":  "node scripts/compress-images.mjs",
    "scan":      "node scripts/scan-images.mjs",
    "build":     "node scripts/scan-images.mjs && node scripts/build-site.mjs",
    "dev":       "node scripts/scan-images.mjs && node scripts/build-site.mjs && node server.js",
    "start":     "node server.js"
  },
  "dependencies": {
    "sharp": "^0.33.5",
    "exifr": "^7.1.3"
  }
}
